// Chakra UI v3 - Configuração simplificada
// A v3 usa uma API diferente baseada em createSystem

// Por enquanto, exportamos um objeto vazio para compatibilidade
// A configuração real deve ser feita via Provider no App.tsx

export const theme = {
  initialColorMode: 'light',
  useSystemColorMode: false,
}

export default theme
