// Fonte única em ../ui/Card. Este arquivo é apenas re-export para os consumidores legados.
export { Card, default } from '../ui/Card';
export type { CardProps, CardVariant, CardSize } from '../ui/Card';
