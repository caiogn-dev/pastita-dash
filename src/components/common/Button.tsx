// Fonte única em ../ui/Button. Este arquivo é apenas re-export para os consumidores legados.
export { Button, default } from '../ui/Button';
export type { ButtonProps, ButtonVariant, ButtonSize } from '../ui/Button';
