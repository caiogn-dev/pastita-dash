# Loop diário — 2026-09-24

Base: `origin/main` em `f4a81b7`.
Validação local (não há CI neste repo): `npx tsc --noEmit` limpo antes e depois;
`npm test` **2026/345 → 2034/346** (o +1 suíte / +8 casos desta fatia);
`npm run lint` mantém os **7 erros pré-existentes** (`Unused eslint-disable` em
`OrdersHeatMap.tsx` ×2, `sidebarColuna.test.tsx`, `pagamentoAMenorAoVivo.test.tsx`
e três `no-var-requires` em `planilhaDoCardapio.test.ts`) — nenhum nos arquivos
tocados, que ficam com 0 erro / 0 warning.

## A exportação de clientes montava o CSV na mão — e o Excel executava o nome

`SubscribersPage.handleExport` (aba **Clientes** do Marketing) virava a lista em
planilha por concatenação crua:

```
`${subscriber.email},${subscriber.name},${subscriber.phone || ''},${subscriber.status}`
```

E-mail, nome e telefone chegam do cliente pelo storefront, então isso reabria os
dois problemas que a exportação de pedidos já tinha resolvido (#203):

- **Column-shift.** Um cliente chamado "Silva, Maria" tem vírgula no meio do
  nome e desloca todas as colunas seguintes; o arquivo abre "certo" e o dono lê
  o telefone na coluna do status sem perceber.
- **Formula injection (OWASP).** Uma célula de texto começando com `= + - @`
  (ou tab/CR) é executada como fórmula pelo Excel/LibreOffice/Sheets ao abrir o
  arquivo — `=HYPERLINK(...)`/`cmd` exfiltram dados na máquina do dono. Um nome
  como `=HYPERLINK("http://mal","clique")` saía cru no CSV.

A defesa é a mesma do `exportarPedidos.ts`, agora extraída para um módulo puro,
testável e reusável: `src/pages/marketing/exportarAssinantes.ts` com
`assinantesParaCsv(assinantes)`. Cada célula de texto ganha prefixo `'` quando
começa com gatilho de fórmula e é aspeada quando contém `,` `"` `\n` `\r` (aspa
interna duplicada). O `handleExport` passou a chamar essa função.

**Mantive o separador `,` e o cabeçalho em inglês** (`email,name,phone,status`)
de propósito: o mesmo formato é relido pela importação da própria tela
(`handleImport` cola texto separado por vírgula e faz `line.split(',')`). Trocar
para o `;` do utilitário canônico `src/utils/csv.ts` quebraria o ida-e-volta, e
o objetivo desta fatia é segurança sem mudar o contrato de importação.

## Teste (TDD)

Novo `src/pages/marketing/__tests__/exportarAssinantes.test.ts` (8 casos): fórmula
neutralizada, gatilhos `+ - @`, vírgula no nome aspeada, aspas internas
duplicadas, CR/LF preso na célula (não abre nova linha executável), telefone
ausente vira célula vazia e o cabeçalho continua `email,name,phone,status`
separado por vírgula (contrato da importação). Vermelho sem o módulo, verde com.

Só um ramo de produção alterado (a montagem do CSV) e um módulo novo puro; risco
baixo, sem mudança de UI nem de rede.

## Follow-up (review do Codex, P2) — importador CSV-ciente

O Codex apontou que a exportação passou a emitir CSV aspeado, mas o
`handleImport` ainda parseava com `split(',')` cru: o ida-e-volta (exportar e
colar de volta em "Importar clientes") corrompia `Ze "Boca"` e quebrava
`Silva, Maria` em duas colunas. Real — regressão que meu escape introduziu no
caso das aspas.

Adicionei `lerContatosCsv`, o inverso de `assinantesParaCsv` no mesmo módulo do
contrato: parser CSV-ciente (aspas com `""` escapado, `,`/CR/LF dentro de campo
aspeado) que desfaz a guarda de fórmula (`'`) só quando foi a exportação que a
pôs. Mantive a leniência do parser antigo (trima campos; só aceita linha com
e-mail contendo `@`, descartando o cabeçalho). +6 testes, incluindo um
exportar→reimportar explícito. `npm test` 2034 → 2040; `tsc` limpo; lint sem
erro novo. Commit `9156746`.
