# =============================================================================
# PASTITA DASHBOARD - ADMIN PANEL CONFIGURATION
# =============================================================================
# Copy this file to .env and fill in your values
# =============================================================================

# -----------------------------------------------------------------------------
# API Configuration
# -----------------------------------------------------------------------------
# Backend API URL (without trailing slash)
# Example: http://localhost:8000/api/v1 or https://api.example.com/api/v1
VITE_API_URL=http://localhost:8000/api/v1

# -----------------------------------------------------------------------------
# WebSocket Configuration
# -----------------------------------------------------------------------------
# WebSocket base URL for real-time notifications
# Should include /ws path prefix (e.g., ws://localhost:8000/ws)
# If not set, will be derived from VITE_API_URL
VITE_WS_URL=ws://localhost:8000/ws

# =============================================================================
# NOTES
# =============================================================================
# 1. All VITE_ variables are exposed to the browser - do not put secrets here
# 2. For production, use HTTPS/WSS URLs:
#    - VITE_API_URL=https://api.example.com/api/v1
#    - VITE_WS_URL=wss://api.example.com/ws
# 3. Make sure CORS is configured on the backend for your dashboard domain
# 4. WebSocket paths available:
#    - /ws/notifications/ - User notifications
#    - /ws/dashboard/ - Dashboard real-time updates
#    - /ws/payments/ - Payment status updates
#    - /ws/orders/ - Order status updates
#    - /ws/chat/{conversation_id}/ - Chat messages
#    - /ws/automation/ - Automation events
# =============================================================================
